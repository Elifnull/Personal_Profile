import SideBar from '../Sidebar';
import './index.scss';

// Setup views here on layout
//build seperate scss files in each components subfolder (namming convention index.js and index.scss)
const Layout = () =>{


    return(
        <>
        <SideBar/> 
        </>
    )
}

export default Layout;